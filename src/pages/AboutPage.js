import React from "react";

export const AboutPage = () => (
  <div class="jumbotron">
    <div class="container">
      <h1 class="display-4">Simple React App</h1>
      <p class="lead">
        An example on a React application (Context, Hooks, Animation, Deploy)
        using the Sass preprocessor and the Bootstrap library.
      </p>
    </div>
  </div>
);
